node_modules
dist
logs
*.pid
.env
.env.local

# tauri (build Rust)
src-tauri/target
src-tauri/gen

# relay
relay/node_modules
relay/logs
relay/*.pid
# Données utilisateur (enveloppes store-and-forward + backups chiffrés) — jamais publiées
relay/data
# Sauvegardes exportées à la main : contenu chiffré, mais ce sont les données de quelqu'un
*.ncb
